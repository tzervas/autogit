# =============================================================================
# AutoGit Core Configuration
# =============================================================================
# Configuration for AutoGit core services (Rust bootstrap, etc.)
# Copy this file to .env.core and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# Domain Configuration
# -----------------------------------------------------------------------------

# Base domain for all services (used by bootstrap.rs for user emails)
BASE_DOMAIN=example.com

# -----------------------------------------------------------------------------
# GitLab Configuration
# -----------------------------------------------------------------------------

# GitLab instance URL (used by bootstrap.rs and core services)
GITLAB_URL=https://gitlab.example.com

# Admin token for user creation (used by bootstrap.rs)
GITLAB_ADMIN_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# Alternative environment variable names (also supported)
# AUTOGIT_GITLAB_URL=${GITLAB_URL}
# AUTOGIT_GITLAB_TOKEN=${GITLAB_ADMIN_TOKEN}

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------

# Log level for core services (optional, defaults to 'info')
LOG_LEVEL=info

# Alternative environment variable name (also supported)
# AUTOGIT_LOG_LEVEL=${LOG_LEVEL}

# -----------------------------------------------------------------------------
# Credentials File
# -----------------------------------------------------------------------------

# Path to store generated credentials (optional, defaults to 'gitlab-credentials.env')
CREDS_FILE=gitlab-credentials.env

# -----------------------------------------------------------------------------
# User Account Configuration (Auto-constructed from BASE_DOMAIN)
# -----------------------------------------------------------------------------
# Service accounts created by bootstrap.rs:
# - autogit-ci (ci@${BASE_DOMAIN})
# - autogit-api (api@${BASE_DOMAIN})
# - autogit-backup (backup@${BASE_DOMAIN})
#
# Human users created by bootstrap.rs:
# - sysadmin (sysadmin@${BASE_DOMAIN}) - Admin user
# - kang (kang@${BASE_DOMAIN}) - Regular user
# - dev (dev@${BASE_DOMAIN}) - Regular user

# -----------------------------------------------------------------------------
# Usage Instructions
# -----------------------------------------------------------------------------
# 1. Copy this file: cp .env.core.example .env.core
# 2. Set your actual values (GITLAB_URL, GITLAB_ADMIN_TOKEN, BASE_DOMAIN)
# 3. Run bootstrap: cargo run --bin bootstrap
# 4. Credentials will be saved to CREDS_FILE path