# AutoGit Environment Configuration Example
# Copy this file to .env and customize for your environment
#
# SECURITY NOTE: Never commit .env to version control!
# The actual .env file contains sensitive credentials.

# ============================================================================
# GitLab Configuration
# ============================================================================

# GitLab root user password
# This is auto-generated on first run and stored in ~/.autogit/gitlab_root_password
# You can also set it manually here before first deployment
GITLAB_ROOT_PASSWORD=

# GitLab HTTP/HTTPS ports
GITLAB_HTTP_PORT=3000
GITLAB_HTTPS_PORT=3443
GITLAB_SSH_PORT=2222

# GitLab external URL
GITLAB_OMNIBUS_CONFIG=external_url "http://localhost:3000"

# ============================================================================
# Runner Coordinator Configuration
# ============================================================================

# Resource limits per runner (enhanced for self-hosted)
RUNNER_CPU_LIMIT=4.0        # CPU cores per runner
RUNNER_MEM_LIMIT=6g         # RAM per runner

# Runner lifecycle management
RUNNER_COOLDOWN_SECONDS=300 # Idle time before cleanup (5 minutes)
RUNNER_POLL_INTERVAL=10     # Job check frequency (seconds)

# GitLab integration (auto-configured by setup script)
GITLAB_URL=http://192.168.1.170:3000
GITLAB_TOKEN=                # Auto-generated
GITLAB_PROJECT_ID=           # Auto-generated

# ============================================================================
# Storage & Network
# ============================================================================

GITLAB_DATA_PATH=/var/lib/autogit
HOMELAB_IP=192.168.1.170
COORDINATOR_URL=http://192.168.1.170:8080

# ============================================================================
# IMPORTANT NOTES
# ============================================================================
#
# 1. Password is auto-generated on first run
# 2. Run: bash scripts/first-time-setup-complete.sh
# 3. Credentials stored in: ~/.autogit/
# 4. Never commit .env with real credentials!
