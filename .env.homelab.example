# =============================================================================
# AutoGit Homelab Deployment Configuration
# =============================================================================
# Configuration for deploying AutoGit to a homelab server
# Copy this file to .env.homelab and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# SSH Configuration
# -----------------------------------------------------------------------------

# SSH connection details for homelab server
HOMELAB_SSH_USER=${USER:-kang}
HOMELAB_SSH_HOST=${HOMELAB_IP:-192.168.1.170}
HOMELAB_SSH_KEY_PATH=${HOME}/.ssh/id_ed25519

# Deployment path on homelab server (auto-constructed)
HOMELAB_TARGET_PATH=/home/${HOMELAB_SSH_USER}/homelab-gitlab

# -----------------------------------------------------------------------------
# Docker Configuration (for homelab deployment)
# -----------------------------------------------------------------------------

# Docker socket path on homelab server
# For rootless Docker: unix:///run/user/1000/docker.sock
# For standard Docker: unix:///var/run/docker.sock
HOMELAB_DOCKER_SOCKET=unix:///run/user/1000/docker.sock

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------

# Homelab server IP address
HOMELAB_IP=192.168.1.170

# -----------------------------------------------------------------------------
# Deployment Configuration
# -----------------------------------------------------------------------------

# Container name for monitoring
CONTAINER_NAME=gitlab

# -----------------------------------------------------------------------------
# Usage Instructions
# -----------------------------------------------------------------------------
# 1. Copy this file: cp .env.homelab.example .env.homelab
# 2. Set your actual homelab server details
# 3. Run deployment: bash homelab-migration/deploy-clean.sh
# 4. Monitor deployment: bash homelab-migration/monitor-startup.sh
