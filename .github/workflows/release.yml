---
name: Release

# This workflow creates releases and publishes Docker images to GHCR.
# It only runs when:
# 1. A version tag (v*.*.*) is pushed to the repository
# 2. Manually triggered via workflow_dispatch (requires version input)
# 3. Push to dev branch (will validate and potentially create release)
#
# Docker Images:
# - Images are pushed to GitHub Container Registry (ghcr.io)
# - Layer caching is enabled using both GHCR and GitHub Actions cache
# - Only changed layers are rebuilt and pushed for efficiency
# - Cache sources: dev images, previous releases, and dedicated cache tags
#
# For testing builds without releasing, use the "Release Test Build" workflow

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., v0.2.0) - must be on dev branch'
        required: true
        type: string
      create_tag:
        description: 'Create tag if it does not exist'
        required: false
        type: boolean
        default: true

jobs:
  validate-release:
    name: Validate Release Requirements
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Validate branch
        run: |
          CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
          echo "Current branch: $CURRENT_BRANCH"

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            if [ "$CURRENT_BRANCH" != "dev" ]; then
              echo "❌ Manual releases must be triggered from dev branch"
              echo "Current branch: $CURRENT_BRANCH"
              exit 1
            fi
            echo "✅ Release triggered from dev branch"
          fi

      - name: Validate version format
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
            if [[ ! "$VERSION" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
              echo "❌ Invalid version format: $VERSION"
              echo "Expected format: vX.Y.Z (e.g., v0.2.0)"
              exit 1
            fi
            echo "✅ Version format valid: $VERSION"
          fi

  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: validate-release
    outputs:
      version: ${{ steps.get_version.outputs.version }}
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Get or create version tag
        id: get_version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
            # Check if tag exists
            if ! git rev-parse "refs/tags/$VERSION" >/dev/null 2>&1; then
              if [ "${{ inputs.create_tag }}" = "true" ]; then
                echo "⚠️  Tag $VERSION does not exist. Creating it now..."
                git config user.name "github-actions[bot]"
                git config user.email "github-actions[bot]@users.noreply.github.com"
                git tag -a "$VERSION" -m "Release $VERSION"
                git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
                git push origin "$VERSION"
                echo "✅ Created and pushed tag $VERSION"
              else
                echo "❌ Tag $VERSION does not exist and create_tag is false"
                exit 1
              fi
            else
              echo "✅ Tag $VERSION already exists"
            fi
          else
            VERSION=${GITHUB_REF#refs/tags/}
            echo "✅ Using tag from push event: $VERSION"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Extract changelog
        id: changelog
        run: |
          VERSION="${{ steps.get_version.outputs.version }}"
          # Extract changelog for this version from CHANGELOG.md
          if [ -f "CHANGELOG.md" ]; then
            # Find the section for this version and extract it
            CHANGES=$(sed -n "/## \[$VERSION\]/,/## \[/p" CHANGELOG.md | sed '$d')
            if [ -z "$CHANGES" ]; then
              CHANGES="No changelog entries found for $VERSION"
            fi
          else
            CHANGES="See commit history for changes in this release."
          fi

          # Write to file to preserve formatting
          echo "$CHANGES" > release_notes.md
          echo "Changelog extracted"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.get_version.outputs.version }}
          name: Release ${{ steps.get_version.outputs.version }}
          body_path: release_notes.md
          draft: false
          prerelease: false
          generate_release_notes: true

  build-and-publish-images:
    name: Build and Publish Docker Images
    runs-on: ubuntu-latest
    needs: create-release
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        service: [git-server, runner-coordinator]
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/${{ matrix.service }}
          tags: |
            type=semver,pattern={{version}},value=${{ needs.create-release.outputs.version }}
            type=semver,pattern={{major}}.{{minor}},value=${{ needs.create-release.outputs.version }}
            type=semver,pattern={{major}},value=${{ needs.create-release.outputs.version }}
            type=raw,value=latest
            type=sha

      - name: Build and push Docker image (with layer caching)
        uses: docker/build-push-action@v6
        with:
          context: ./services/${{ matrix.service }}
          file: ./services/${{ matrix.service }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: |
            type=registry,ref=ghcr.io/${{ github.repository }}/${{ matrix.service }}:cache
            type=registry,ref=ghcr.io/${{ github.repository }}/${{ matrix.service }}:dev
            type=registry,ref=ghcr.io/${{ github.repository }}/${{ matrix.service }}:latest
            type=gha
          cache-to: |
            type=registry,ref=ghcr.io/${{ github.repository }}/${{ matrix.service }}:cache,mode=max
            type=gha,mode=max

      - name: Image published
        run: |
          echo "✅ Published ${{ matrix.service }} image"
          echo "Tags: ${{ steps.meta.outputs.tags }}"
