╔══════════════════════════════════════════════════════════════════════════════╗
║                    AutoGit Runner Coordinator Deployed!                       ║
╚══════════════════════════════════════════════════════════════════════════════╝

Thank you for installing {{ .Chart.Name }}.

Your release is named: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 CONFIGURATION SUMMARY:

  GitLab URL:          {{ .Values.config.gitlabUrl }}
  Cooldown Minutes:    {{ .Values.config.cooldownMinutes }}
  Max Idle Runners:    {{ .Values.config.maxIdleRunners }}
  Executor Type:       {{ if .Values.executor.kubernetes.enabled }}kubernetes{{ else }}docker{{ end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 GET THE APPLICATION STATUS:

  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "runner-coordinator.name" . }}"

📊 VIEW LOGS:

  kubectl logs -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "runner-coordinator.name" . }}" -f

🌐 ACCESS THE SERVICE:

{{- if .Values.ingress.enabled }}
  URL: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ (index .Values.ingress.hosts 0).host }}
{{- else }}
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "runner-coordinator.fullname" . }} 8080:{{ .Values.service.port }}
  Then visit: http://localhost:8080
{{- end }}

🔐 SECRETS:
{{- if .Values.existingSecret }}
  Using existing secret: {{ .Values.existingSecret }}
{{- else }}
  Secret created: {{ include "runner-coordinator.fullname" . }}
  
  ⚠️  WARNING: If you didn't set gitlabToken and runnerRegistrationToken,
      the coordinator will have limited functionality. Update the secret:
  
  kubectl create secret generic {{ include "runner-coordinator.fullname" . }} \
    --from-literal=GITLAB_TOKEN=<your-token> \
    --from-literal=GITLAB_RUNNER_REGISTRATION_TOKEN=<your-token> \
    -n {{ .Release.Namespace }} --dry-run=client -o yaml | kubectl apply -f -
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 DOCUMENTATION:
   https://github.com/tzervas/autogit/blob/main/docs/runners/

🐛 ISSUES:
   https://github.com/tzervas/autogit/issues

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
