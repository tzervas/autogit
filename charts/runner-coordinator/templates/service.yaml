apiVersion: v1
kind: Service
metadata:
  name: {{ include "runner-coordinator.fullname" . }}
  labels:
    {{- include "runner-coordinator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "runner-coordinator.selectorLabels" . | nindent 4 }}
