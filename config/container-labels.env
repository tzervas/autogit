# =============================================================================
# AutoGit Container Image Configuration
# =============================================================================
# This file defines standardized naming conventions and labels for all
# container images built in the AutoGit project.
#
# NAMING CONVENTION:
# {registry}/{owner}/{repo}/{component}:{version}-{arch}-{variant}
#
# Examples:
#   ghcr.io/tzervas/autogit/runner-coordinator:0.2.0-amd64-release
#   ghcr.io/tzervas/autogit/git-server:0.2.0-amd64-dev
#   192.168.1.170:5050/root/autogit/runner-coordinator:latest-amd64
#
# VERSIONING:
#   - Release: v{major}.{minor}.{patch} (e.g., v0.2.0)
#   - Dev: v{major}.{minor}.{patch}-dev.{timestamp}
#   - Feature: {branch}-{short-sha}
#   - Latest: latest (always points to most recent stable)
#
# ARCHITECTURE:
#   - amd64: Intel/AMD 64-bit (primary)
#   - arm64: ARM 64-bit (Apple Silicon, AWS Graviton)
#   - multi: Multi-arch manifest
#
# VARIANT:
#   - release: Production-ready, from main branch
#   - dev: Development builds, from dev branch
#   - feature: Feature branch builds
#   - nightly: Scheduled nightly builds
# =============================================================================

# Project Information
PROJECT_NAME=autogit
PROJECT_VENDOR=AutoGit
PROJECT_OWNER=tzervas
PROJECT_REPO=tzervas/autogit

# Documentation URLs
DOCS_URL=https://github.com/tzervas/autogit
SOURCE_URL=https://github.com/tzervas/autogit
ISSUES_URL=https://github.com/tzervas/autogit/issues
WIKI_URL=https://github.com/tzervas/autogit/wiki

# License
LICENSE=MIT

# Registries
GHCR_REGISTRY=ghcr.io
GITLAB_REGISTRY=192.168.1.170:5050

# Image Names (component only - full path built dynamically)
GIT_SERVER_IMAGE=git-server
RUNNER_COORDINATOR_IMAGE=runner-coordinator

# Component Descriptions
GIT_SERVER_DESCRIPTION="GitLab CE server with integrated container registry for the AutoGit self-hosted CI/CD platform"
RUNNER_COORDINATOR_DESCRIPTION="Dynamic GitLab runner lifecycle manager with auto-scaling and intelligent resource allocation"

# Component Documentation
GIT_SERVER_DOCS=https://github.com/tzervas/autogit/blob/main/docs/git-server/README.md
RUNNER_COORDINATOR_DOCS=https://github.com/tzervas/autogit/blob/main/docs/runners/AUTONOMOUS_RUNNERS.md

# Default Architecture
DEFAULT_ARCH=amd64

# =============================================================================
# OCI Image Spec Labels (https://github.com/opencontainers/image-spec)
# These are set dynamically at build time using build args
# =============================================================================
# org.opencontainers.image.created        - Build timestamp (ISO 8601)
# org.opencontainers.image.authors        - Maintainer info
# org.opencontainers.image.url            - URL for more info
# org.opencontainers.image.documentation  - Documentation URL
# org.opencontainers.image.source         - Source code URL
# org.opencontainers.image.version        - Version of the image
# org.opencontainers.image.revision       - Git commit SHA
# org.opencontainers.image.vendor         - Organization/company name
# org.opencontainers.image.licenses       - License expression
# org.opencontainers.image.ref.name       - Reference name (tag)
# org.opencontainers.image.title          - Human-readable title
# org.opencontainers.image.description    - Human-readable description
# org.opencontainers.image.base.name      - Base image name
# =============================================================================
